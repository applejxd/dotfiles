version: '3'

services:
  clion:
    image: local/clion:test
    build:
      context: .
      dockerfile: ./clion.dockerfile
    restart: always

    #######
    # SSH #
    #######
    ports:
     - "50023:2222"

    #######
    # GUI #
    #######
    # network_mode: "host"
    environment:
      - DISPLAY
    volumes:
      # - /tmp/.X11-unix:/tmp/.X11-unix:rw"
      - $HOME/.Xauthority:/root/.Xauthority:rw
    
