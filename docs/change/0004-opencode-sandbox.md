# CHG-0004: OpenCode の保護を OS のアクセス制御へ移す

- **状態**: In progress
- **更新日**: 2026-09-23
- **基準**: OpenCode V2（`v2.0.12`）/ `@anthropic-ai/sandbox-runtime` v0.0.76
- **対象 OS**: **Ubuntu / WSL のみ**（macOS と Windows は [CHG-0002](0002-opencode-ask-by-default.md) の方針を維持）

## 目的と非目的

**目的**: 列挙型の規則では任意コード実行の権限境界を作れないので、
**OS のアクセス制御を保護の主役にする**。自動許可は事前承認した作業範囲に
限り、実ホームへの適用と外部への作用は別の権限として扱う。

**非目的**:

- macOS / Windows の設計変更。bwrap が無いので現行方針を維持する
- Claude Code / Copilot CLI の設計変更
- **「sandbox を入れたので全部 `allow`」にすること**。境界はファイルの話で、
  通信・認証情報・公開操作は別に扱う（[判断の根拠](#なぜ切り替えるか)）

## 発端

[CHG-0002](0002-opencode-ask-by-default.md) は「境界は無い」という前提で
plugin に安全網を積んできた。段階 3 まで配備した時点で次が見えた。

| 観測 | 内容 |
| --- | --- |
| 規則が単調増加している | 誘導 5 本・伏字化 9 本・パターン 71 本。誤爆のたびに除外条件が増える |
| 誤爆の原因が構造的 | 「コマンドとして使う」と「文章として書く」をシェルの構文解析なしに区別できない。1 日で 3 件踏み、うち 1 件は**コミットそのものができなくなった** |
| **書き込み保護が成立していない** | write deny の 38 glob は `edit` ツールにしか効かず、shell のリダイレクトには**一切効かない** |
| 塞げない経路が残る | `printf` / `tee` / `python3 -c "open(...)"`、`> $VAR`、`base64` / `tr` |

決定打は 3 番目。**保護の中核が動いていなかった**。

## なぜ切り替えるか

判定の問いが変わる。

| | 列挙型 | 境界がある場合 |
| --- | --- | --- |
| 各コマンドに問うこと | このコマンドは何ができるか（**shell では答えられない**） | 効果は境界の外に出るか |

後者は答えられる。列挙の対象が「許すもの（約 300 件）」から
**「越えるもの（10 件程度）」**へ縮む。

### 境界を過信しない

外部レビュー（Astra）の指摘を採る。**sandbox は許した範囲内の事故を防がない。**

| 危険 | 境界で防げるか |
| --- | --- |
| 秘密の読み出し・持ち出し | **防げる** |
| 境界外への書き込み | **防げる** |
| 未コミット成果物の破壊 | **境界は防げない**。起動前の退避が網（[段階 6](#段階-6-利便性コストの棚卸しと是正2026-09-23)） |
| ネットワーク経由の送信 | 許可ドメインの範囲でのみ可能 |
| リポジトリ自身への細工 → 後日の `chezmoi apply` で実環境へ | **防げない**。適用時の差分確認が唯一の網 |

最後が効く。**このリポジトリは「ホーム全体を書き換える source state」**なので、
境界内で自由に書けることは「後で実環境へ反映される内容を自由に書ける」ことと
同義になる。だから `chezmoi apply` を境界の外に置き、そこで差分を見る。

## 期待される効果

移行後の見込み（実履歴 1,128 呼び出しで算定）。

| 区分 | 対処前 | **対処後** |
| --- | --- | --- |
| 自動で進む | 91.6% | **98.7%** |
| 承認で止まる | 1.3% | **1.3%** |
| 失敗で止まる | 7.1% | ほぼ 0% |

**承認が要るのは `chezmoi apply` / `re-add`（13 件）と `git push`（2 件）だけ。**
どちらも意図的に人が見る操作なので、残すことが目的にかなう。

「対処」はドメイン許可リストへの追加と読み取り専用 PAT の配置（段階 4）。

上乗せの待ち時間は**コマンドごとには掛からない**（隔離は起動時 1 回）。

> **「自動」は「境界内で成功する」前提。** ワークスペース・`/tmp`・`~/.cache`
> 以外へ書くコマンドは失敗する。実運用で洗い出す。

## 実施計画

| 段階 | 内容 | 状態 |
| --- | --- | --- |
| 0 | `srt` の成立確認と構成の決定 | **完了**（2026-09-22） |
| 1 | `chezmoi` を snap から外す | **完了**（2026-09-22） |
| 2 | **`opencode --standalone` ごと `srt` で包むランチャーを作る** | **完了**（2026-09-23） |
| 3 | 隔離版の設定系統を分け、規則を作り替える | **概ね完了**（対応表が未作成） |
| 4 | 許可リスト（パス・ドメイン）を `common.toml` で単一ソース化 | **完了**（2026-09-23） |
| 5 | 境界内で無意味になった層だけを削る（改訂） | **完了**（2026-09-23。削減はゼロ） |
| 6 | **利便性コストの棚卸しと是正** | **完了**（2026-09-23。保留 4 件） |

> **構成が変わった（2026-09-23）。** 当初は「plugin が shell の実行ごとに包む」
> 設計だったが、**実行主体ごと包めることが実測で分かった**ので差し替えた。
> 経緯は[調査記録の 19 節](../research/opencode/permission/sandbox-runtime.md)。

### 段階 0: `srt` の成立確認と構成の決定（完了）

`@anthropic-ai/sandbox-runtime` は Claude 専用ではなく**汎用**で、seccomp の
ために**既に導入済み**だった。実地で次を確認した
（[調査記録](../research/opencode/permission/sandbox-runtime.md)）。

| 検査 | 結果 |
| --- | --- |
| `~/.ssh` / `~/.config/gh` / `~/.git-credentials` | 拒否 |
| ワークスペースの読み書き | 成功 |
| **`git push`** | **`could not read Username`** |
| **`gh auth status`** | **未ログイン扱い** |
| 許可ドメイン / 許可外ドメイン | `200` / **遮断** |
| `cmd.exe`（WSL の脱出経路） | **拒否**（seccomp） |
| **`chezmoi`（snap）** | **internal error** |

**認証情報を見せないだけで `git push` と `gh` の書き込みが止まる。**
コマンド文字列の判定は 1 つも要らない。

#### 構成の決定: サービスではなく**実行ごと**に包む

当初は `opencode serve --service` を包む想定で、**いったん不成立と判断した**。

サービスは TCP `127.0.0.1` で待ち受け、TUI がそこへ接続する。Linux の `srt` は
**ネットワーク名前空間を丸ごと除去する**ため、内側で待ち受けても外から届かない
（実測: 内側の `bind` は成功、外側からの接続は `TimeoutError`）。

> **この結論の一般化は誤りだった（2026-09-23 に訂正）。** 当てはまるのは
> **境界をまたぐ構成だけ**。`opencode --standalone` は TUI とサーバが
> **同一プロセス**なので、またぐ接続が発生しない。実測で成立した
> （[調査記録 19 節](../research/opencode/permission/sandbox-runtime.md)）。
>
> ネットワーク名前空間が除かれる性質は、境界としては**利点**でもある。
> 内側の OpenCode は**外側の共有サービスへ到達できない**ので、
> 「TUI だけ包んで共有サーバでツールを実行する」抜け道が自動的に塞がる。

この訂正により、途中で検討した 2 案は**どちらも破棄**した。

| 破棄した案 | 破棄の理由 |
| --- | --- |
| A: plugin が `execute.before` でコマンドを差し替える | 後続の権限評価が**書き換え後**を見るため、静的 allow 5 件と deny が一致しなくなる（実測） |
| B: 設定の `shell` にラッパーを指定する | 権限評価は生き残るが、**コマンド文字列で sandbox を解除する**構造になる。外部レビューが「認可の設計として不適切」と判定 |

**この構成の代償**は次の 2 点。計画に織り込む。

| 代償 | 影響 |
| --- | --- |
| ワークスペースは境界が守らない | 段階 3 で `allow` + snapshot による復旧として扱う |
| **保護機構そのものが書き換えられる** | 権限設定もランチャーのソースも全て書ける（実測）。`denyWrite` と**人間の差分審査**で守る |

#### 分かった癖（導入時の注意）

- **`allowWrite` の祖先を `allowRead` に載せると無効化される**（内側で `EROFS`）。
  書きたい領域は**根ごと** `allowWrite` に載せ、祖先を `allowRead` に載せない。
  完全一致なら問題ない
- **どちらにも載っていない領域への書き込みは、成功したように見えて消える。**
  最も危険。設定を書いたら必ず実体を確認する
- **`denyRead` は「広く deny → `allowRead` で戻す」でしか効かない。**
  ピンポイントの deny は黙って無視される
- `denyWrite` はディレクトリ単位で機能し、`allowWrite` より優先される
- 広い `denyRead` の下では `$HOME` が差し替わる。`$HOME` 直下に書けても
  実ホームには現れない（境界の破れと誤読しない）
- PATH の `srt` は**別物**（Python の字幕処理ツール）。実体を直接叩く
- 版は `latest` の浮動指定で **Claude Code と共有**している。片方の都合で
  上がると両方の挙動が変わる

規則の導出は[調査記録](../research/opencode/permission/sandbox-runtime.md)。

#### 復旧経路は境界の外だが、**同じ作業コピーを開いてはいけない**

当初は、境界の内側から抜けるための `bypass` エージェントを用意する計画だった。
**実行主体ごと包む構成では要らない。** 境界はランチャーが張るので、
ランチャーを使わなければそれが境界の外になる。

**ただし「通常版を起動して直す」は、そのままでは危険。**

OpenCode は起動時にプロジェクト設定を探索し、設定には plugins・MCP・
formatter・LSP という**実行経路**がある。隔離側が変更した作業コピーを
通常版で開くと、**審査に入る前にそのコードが動きうる**。

| やってはいけない | 代わりに |
| --- | --- |
| 未審査の作業コピーを通常版の OpenCode で開く | **テンプレートを評価せず、データとして読む** |
| **未審査のまま `chezmoi diff` を実行する** | テンプレートと参照先を審査した**後**に実行する |
| 作業コピー内の mise タスクや検証スクリプトを先に走らせる | 審査を済ませてから |
| ランチャーを作業コピーから起動する | `~/.local/bin/` の実体から起動する |

##### 審査コマンド自体が実行経路になる

**`chezmoi diff` は安全な審査手段ではない。** テンプレートを評価するため、
`output` 関数が**外部コマンドを実行する**。未審査の `.tmpl` に対して
ホストで実行すれば、避けたかった「審査前のホスト実行」が別経路で成立する。

`git diff` も無条件に受動的ではない。**外部 diff・textconv・pager** の設定
次第で外部プログラムを起動し、その設定は作業コピー側から変更できる
（`.git/config` を残す理由でもある）。

したがって審査は 2 段にする。

| 段 | 内容 |
| --- | --- |
| 1 | **テンプレートを評価せず、データとして読む。** Git の未信頼設定による外部プログラム起動も排除する |
| 2 | テンプレートと参照先の審査が済んでから `chezmoi diff` を実行する |

審査対象は `git diff` の出力に限らない。**固定した信頼済み基準からの変更**と、
**未追跡ファイルを含む展開対象全体**を見る。

**審査は、対象の設定・plugin・タスクを読み込まない環境で行う。**
審査した内容と、実際にホストで実行・展開する内容を一致させる
（審査後に隔離側が書き換えたものを適用しない）。

> `bypass` は subagent として起動できない（実測で
> `Agent bypass cannot run as a subagent` を確認）。
> [bypass エージェント](../research/opencode/permission/bypass-agent.md)の記録は
> 残すが、この計画では使わない。

#### ランチャーが境界を張るまでの信頼の鎖

**境界が成立する前にホストで動くものは、すべて信頼対象。**
ここに可変な入力が混ざると、保護は境界の前で迂回される。

| 守ること | 理由 |
| --- | --- |
| ランチャーは**絶対パス**で実体を呼ぶ | `PATH` の差し替えを防ぐ |
| **mise タスクを経由しない** | タスク定義はワークスペース内＝可変 |
| 設定・チェックスクリプトも `allowRead` 側の実体を参照する | 作業コピーを読ませない |
| シェル起動設定（`.bashrc` 等）に依存しない | 可変 |

`denyWrite` の対象は、この鎖を辿って**導出する**。ファイル名の列挙では足りない。

| 辿る先 | 例 |
| --- | --- |
| 生成器とその import 先・テンプレート | `scripts/agents/generate.py` とその依存 |
| 設定の参照先 | `common.toml` の該当入力 |
| 起動チェック自体 | チェックスクリプトの実体 |
| 実行ファイルの解決 | `PATH`、ランチャーが呼ぶ各バイナリ |

**直接編集だけでなく、再生成・親ディレクトリの置換・rename・参照先の
差し替えによる迂回も検証対象**にする。

### 段階 1: `chezmoi` を snap から外す（完了）

snap 版は bwrap 内で動かない。ブートストラップの 1 行を差し替えた。

```sh
# README の変更
- sudo snap install chezmoi --classic
+ sh -c "$(curl -fsLS get.chezmoi.io)" -- -b "$HOME/.local/bin"
```

**mise 先行には変えない。** `chezmoi` が `mise` を入れる一方向の依存を保つ。
逆にすると `chezmoi apply` の最中に mise 自身を更新する場面が自己参照になる。

#### snap を外して分かったこと（2026-09-22）

**`chezmoi = "latest"` は既に mise で宣言されていた**（`config.toml.tmpl`）。
`/snap/bin` が PATH で mise の shim より前にあり、**それを覆い隠していただけ**
だった。snap を外した時点で mise 管理版へ切り替わった。

したがって公式インストーラが要るのは**初回の bootstrap だけ**で、以降は
mise が引き継ぐ。`~/.local/bin` のコピーは残さなくてよい（重複するので削除した）。

#### 完了の確認

| 確認 | 結果 |
| --- | --- |
| 削除前の安全性（`status` / `diff` の出力） | snap 版と公式版で**完全一致**（151 行） |
| 設定の置き場 | `~/.config/chezmoi/`。snap の外にあり失うものなし |
| 現在の解決先 | `~/.local/share/mise/installs/chezmoi/latest/chezmoi` |
| **境界内での動作** | `--version` / `source-path` / `execute-template` / `data` **全て成功** |

`chezmoi apply` の実行は利用者の判断に委ねる（`status` に未適用の差分がある）。

### 段階 2: `opencode --standalone` ごと `srt` で包む（完了）

このリポジトリ専用のランチャーが、境界を張ってから OpenCode を起動する。

```sh
srt -s <境界の設定> -c "opencode --standalone"
```

`--standalone` は **TUI とサーバが同一プロセス**なので、境界をまたぐ接続が
発生しない。コマンド文字列に一切触れないので、**permission の allow / deny が
そのまま生きる**。

| 得られるもの | 内容 |
| --- | --- |
| 覆う範囲 | shell だけでなく `read` / `edit`・plugin・LSP・formatter も境界内 |
| 固定費 | **コマンドごとには掛からない**（隔離は起動時 1 回） |
| 外側の共有サービス | ネットワーク名前空間の除去で**到達できない**（実測） |

#### 境界の設定

| 区分 | 対象 |
| --- | --- |
| `allowWrite` | ワークスペースの根 |
| `allowRead` | `opencode` 本体、mise、`~/.local/bin`、`~/.config/opencode`、**プロジェクトごとの許可パス** |
| `denyRead` | `/home/<user>`、**`/mnt`**、**`/tmp`**、`/var/tmp`、`/dev/shm` |
| `network` | 許可ドメインのみ（proxy 経由） |

`denyRead` は「広く deny → `allowRead` で戻す」形で書く。ピンポイントの deny は
**黙って無視される**。

> **`/mnt` と `/tmp` は実測で開いていた。** `denyRead: /home/<user>` は
> **WSL の Windows 側を守らない**。`/mnt/c/Users` まで読めていた。
> `/tmp` はホストと共有されていた。どちらも `denyRead` に入れて塞ぐ。
> `/tmp` は**空の書き込み可能な tmpfs で覆われる**ので道具は壊れない。

#### 安全網の保存先（`XDG_DATA_HOME`）

**これを外すと安全網が消える。** snapshot の実体は
`$HOME/.local/share/opencode/` に置かれるが、そこは境界内では**隠されて
消える領域**なので、境界を抜けた瞬間に復元用データが失われる。

捕捉は成功したように見えるため、**気づけない**。`XDG_DATA_HOME` を
`allowWrite` 配下へ向けること。実測で復元まで確認した。

#### 隔離用の DB

`--standalone` は専用サーバを意味するが**専用 DB は意味しない**。
共有すると他プロジェクトの会話が境界内へ入る。`OPENCODE_DB` で分ける。

作り方は「実 DB を複製 → 資格情報と `migration` 以外を消す → `vacuum`」。
**`migration` を消すと壊れる**（スキーマ管理の記録）。

#### ランチャーの置き場と保護

**ワークスペースに置いてはいけない。** 書き換えられたうえで次回**外側で**
実行されると、境界の前にコードが走る。

| 対象 | 置き場 | 内側から |
| --- | --- | --- |
| ランチャー本体 | `~/.local/bin/` | `allowRead` のみ → 書けない |
| 境界の設定 | `~/.config/opencode/` の隣 | 同上 |
| それらのソース | ワークスペース（chezmoi source state） | 編集できる |

chezmoi 管理のままにする。AI はソースを提案できるが、**自分で有効化できない**。

`denyWrite` の対象は「**次回の隔離起動時に、実行コード・境界設定・緩和設定の
選択を決定する入力一式**」とする。ファイル名の列挙では足りない。
生成器（`generate.py`）や `common.toml` の該当入力、起動経路を決める
mise タスクも含む。**保護対象を、変更できる生成器から次回上書きできるなら
保護は迂回されている。**

#### 起動時の境界チェック（fail-closed）

**値は根拠にしない。** `/proc/self/status` の `Seccomp:2` は「何らかの filter が
付いている」ことしか示さず、親環境由来のものでも同じ値になる。

**拒否されるはずの操作を実際に試し、1 つでも通ったら起動を中止する。**

| # | 確認 | 検出する劣化 |
| --- | --- | --- |
| 1 | `~/.ssh` / `~/.git-credentials` / `~/.config/gh` が読めない | `denyRead` の漏れ |
| 2 | `/mnt/c/Users` / `/mnt/c/Windows` が読めない | WSL の Windows 側 |
| 3 | ホストの unix socket へ繋がらない | **seccomp の実効性** |
| 4 | 許可外ドメインへ出られない | proxy の設定漏れ |
| 5 | `XDG_DATA_HOME` が永続領域にある | 安全網の消失 |
| 6 | 作業領域へ書ける | 設定ミスで作業不能になるのを防ぐ |
| 7 | **許可外への書き込みが拒否される** | `allowWrite` の広すぎ |
| 8 | **`denyWrite` の保護対象を変更できない** | 保護機構の自己改変 |

##### 「拒否された」と「試験できなかった」を区別する

**混同すると、穴が開いているのに合格になる。** 対象が存在しない・
接続先が止まっている・名前解決に失敗した場合も「繋がらなかった」に見える。

**エラーコード単独で合否を決めてはいけない。**

| 誤りやすい判定 | 理由 |
| --- | --- |
| `ECONNREFUSED` を隔離の証拠にする | **サービスが待ち受けていないだけ**かもしれない |
| `EACCES` を隔離の証拠にする | 元からのアクセス権による拒否かもしれない |
| `ENOENT` を失敗と見なす | **対象を隠す方式では、これが正しい隔離結果**（今回の `denyRead` がそう） |

したがって次の形で判定する。

| 要件 |
| --- |
| 試験対象は**存在することが分かっている無害なもの**を使う |
| **対照条件（境界の外）では利用できる**ことを確かめ、内側の結果と対応付ける |
| 採用した隔離方式で**期待される結果**を事前に決め、それと突き合わせる |
| **原因を判別できなければ停止する**（判定できない ≠ 合格） |

ネットワークの試験は、**管理下の無害な待受先**を使い、単なるサービス停止を
合格扱いしない。具体的な試験対象の選定は実装時でよい。

##### 永続性は隔離をまたいで確かめる

**5 は内側だけでは確認できない。** 隔離の中では「書けた」ことしか分からず、
外へ出たときに残るかは別（これが実際に踏んだ罠）。
**隔離の終了後・再起動後に実体を検査する**手順を持つ。

##### チェック後に条件が変わらないこと

事前チェックと本体実行で設定や入力が違えば、検査した意味がない。
**同じ設定・同じ入力で本体を起動する**（チェック後に再生成しない）。

実測で、硬化した設定は合格・弱い設定は不合格になり、**`srt` は内側の終了コードを
そのまま外へ伝える**（`exit 42` → `42`）。fail-closed が成立する。

> **チェックの結果をパイプへ通さないこと。** `| grep` を挟むと終了コードが
> パイプ末尾のものに置き換わり、**失敗を取りこぼす**（実際に踏んだ）。

#### `chezmoi apply` は境界の外（人間の責務）

境界の内側からは実行できない（ホーム全体への書き込みが要るため）。
**コマンドを例外として通す案は採らない。** 完全一致にしても、
`chezmoi apply` が何をするかは source state とスクリプトが決めるので、
エージェントがそれらを編集できる以上、1 回の承認で書き換えた内容が実行される。

- エージェントは **source state の変更まで**
- **適用は差分確認のうえ人間が行う**

**`chezmoi apply` は単なるファイル複製ではない。** 変更されたコードを
ホスト権限へ移す操作なので、審査の対象には設定だけでなく**実行されるコード**
（`.chezmoiscripts/`・mise タスク・シェル起動設定・Git hooks・ランチャー）が入る。

#### この構成が覆わないもの

| 残余リスク | 内容 |
| --- | --- |
| **審査前のホスト実行** | mise タスク・Git hooks・検証スクリプトは `chezmoi apply` より先に外側で動きうる。**通常版で作業コピーを開くだけでも起きる**（上記の審査手順で塞ぐ） |
| ワークスペースの破壊 | 境界は作業場を守らない（段階 3 で扱う） |
| 境界内の資格情報 | モデルの OAuth は境界内から読める（下記） |
| 許可ドメイン経由の送信 | ドメイン許可は**送信内容も相手アカウントも制限しない** |
| 計算資源 | bwrap は CPU・メモリ・ディスク・課金の上限を保証しない |

##### 境界の外で実行されるものを棚卸しする

**「内側から到達できない」は D-Bus 1 つの拒否からは一般化できない。**
有効な各ツールについて、**実際の実行場所**と**使う資格情報**を数え上げ、
境界の外で動くものは独立した権限として扱う。

| 洗う対象 | 備考 |
| --- | --- |
| `/run` 配下の他の unix socket、**抽象 unix socket**、継承した FD | 3 の試験対象を D-Bus だけにしない |
| localhost・ホスト側アドレスの既存サービスや実行 API | TCP 直結は塞がる（実測）が、それ以外は未確認 |
| **リモート MCP・ブラウザ操作など、隔離の外で処理されるツール** | 境界内から呼べても**実行はホスト側**になりうる |
| WSL の Windows 相互運用 | interop は塞がっていた（実測）。版が変われば再確認 |
| `/proc` 経由の他プロセス情報・FD | 未確認 |

**完了条件**: 通常の作業が境界内で回り、起動時チェックが合格し、
境界を弱めるとチェックが不合格になることを実地で確認できること。
外で実行されるツールの棚卸しが済んでいること。

#### 完了の確認（2026-09-23）

| 確認 | 結果 |
| --- | --- |
| 起動時チェック | **全 13 項目合格**・終了コード 0・所要 2.9 秒 |
| 境界を弱めたとき | 不合格で**起動を中止**（`/mnt` を開けた設定で検出） |
| ランチャー経由の通し起動 | **TUI が起動した** |
| 境界の実在（起動後） | `ls ~/.ssh` が失敗する |

#### 実地でしか出なかった不具合（3 件）

設計と単体の検証では見つからず、**実際に起動して初めて出た**もの。

| 症状 | 原因 | 教訓 |
| --- | --- | --- |
| DB 作成直後に停止 | `node` を mise の **shim** から解決していた。shim は `mise` 本体への symlink で、呼ぶたびに版解決とネットワーク確認が走る | **信頼の鎖の要件（mise を経由しない）に実装が違反していた。** 文書に書いた規律が実装で守れているかは別途確かめる |
| 2 回目以降、必ずゲートで落ちる | ランチャーが作る `.opencode-sandbox/` を ignore していなかった | 生成物は ignore する |
| ネットワークの項目だけ不合格 | srt の proxy（`localhost:3128`）は**境界の生成直後はまだ待ち受けていない**。1 回目の要求が `rc=7` で落ち、対照条件が失敗する | 起動待ちを入れる（最大 30 秒リトライ） |

3 件目を追ったことで、**`api.githubcopilot.com` が許可リストに無く `403 CONNECT tunnel failed` になる**ことも判明した。そのままではチェックを通してもモデルへ到達できなかった。

> **「判別できなければ止める」が働いた。** proxy 未起動を「到達しない＝合格」と
> 読んでいたら、穴が開いたまま素通りしていた。エラーコード単独で判定しない
> という外部レビューの指摘が、実地で効いた事例。

#### 起動後に再現した既知の問題

`ls ~/.ssh` に対しモデルが**「存在しない」と報告した**。境界が隠していることは
伝わらない（[調査記録 20 節](../research/opencode/permission/sandbox-runtime.md)）。
段階 3 のシステムプロンプトで対処する。

### 段階 3: 隔離版の設定系統を分け、規則を作り替える（概ね完了）

#### 設定を 2 系統に分ける

**緩和を隔離版だけに閉じ込める。** 通常版まで緩めてはいけない。

| 系統 | 設定の場所 | 規則 |
| --- | --- | --- |
| 通常版 | `~/.config/opencode/` | 現行 225 件を維持 |
| 隔離版 | `OPENCODE_CONFIG_DIR` が指す別ディレクトリ | 緩和する |

どちらも `common.toml` から生成する。**ディレクトリを分けただけでは足りない**
ので、実効設定・policy・DB が本当に隔離版になっていることを版ごとに確認する。

#### 規則の作り替え（225 件の全件対応）

現物は deny 193 / ask 27 / allow 5。処遇を**全件に割り当てる**
（取りこぼし 0 を機械的に検査する）。

| 分類 | 件数 | 処遇 | 根拠 |
| --- | --- | --- | --- |
| ホスト絶対パスの秘密（`~/.ssh` など） | 42 | **捨てる** | `denyRead` が到達させない |
| **ワークスペース相対の秘密 glob**（`*secret*` `*/.ssh/*` など） | 約 60 | **残す** | **OS 境界はワークスペースを守らない** |
| **`.git/hooks/*` / `.git/config`** | 4 | **残す** | **後でホストで実行されるコード** |
| `systemctl` / `sudo` / `mkfs` / `crontab` | 17 | **捨てる** | 境界内から到達できない |
| `git config --global/--system` | 2 | **捨てる** | 境界外の設定を触れない |
| `git config --local` | 7 | **残す** | hooks / filter で後日のホスト実行に関わる |
| `docker` / `aws` / `kubectl` など | 13 | **残す** | 到達性（socket・リモート・環境変数）の確認が済むまで |
| `git push` / `gh release` / `gh repo delete` | 13 | **残す** | 運用上の禁止。資格情報の有無とは別 |
| 履歴破壊（`reset --hard` / `rebase` / `filter-branch`） | 9 | **残す** | 境界内の履歴を壊せる |
| `.env` の read / edit | 4 | **残す** | 境界内の秘密 |

##### 「残す」規則の強さを区別する

**permission は任意コードへの境界ではない。** `policies` について認めたことを、
permission にも一貫して適用する。

| 強さ | 意味 | 例 |
| --- | --- | --- |
| **OS でも強制** | shell・任意プログラムからも阻止される | ホスト絶対パスの秘密（`denyRead`） |
| **ツール操作上の禁止のみ** | `read` / `edit` ツールは止まるが、**shell からは読める・書ける** | ワークスペース相対の秘密 glob、`.git/hooks/*` |

対応表には**この区別を列として持たせる**。「残したから守られている」と
読めてはいけない。

本当に読み出し・改変を阻止したい対象は、**OS 側でも保護するか、
そもそも作業コピーに含めない**。検証も、専用ツールだけでなく
**shell 経由の読み書きの両方**で行う。

> **snapshot・DB・ログにできた複製にも経路がある。** 元のパスを保護しても、
> 複製を読める経路は別に確認する。

##### 対応表に持たせる項目

前回の指摘（元 225 件との対応が追えない）を満たすため、各規則に次を持たせる。

| 項目 |
| --- |
| 元規則の action と resource |
| 残す / 統合 / 削除と、その理由 |
| **強さ**（OS でも強制 / ツール操作上の禁止のみ） |
| 代替する保護、または受容する喪失 |
| 変更後の規則との対応と、**マージ順序を踏まえた期待動作** |

> **「大幅削減」ではない。** 当初は 20 件程度まで減ると見込んだが、
> **秘密の deny の多くはワークスペース相対の glob** で、境界の外の話ではない。
> 現実的な着地は 130〜150 件。

#### ワークスペースの破壊は `allow` + 復旧で扱う（ただし保証ではない）

**OS 隔離はワークスペースを守らない。** `git reset --hard` を deny にしても、
`rm`・スクリプト・通常の書き込みで同じ破壊ができる。守るべき優先順位は
**まず復元できない未コミットの作業**で、履歴ではない。

確認を挟まずに復旧手段を持つ形にする。

| 確認 | 実測 |
| --- | --- |
| snapshot は **shell の `rm`** を捕捉するか | **する**（ファイルシステム単位のため） |
| **未追跡・未コミット**も戻るか | **戻る**（2 MiB まで） |
| 復元 | `/undo`、または revert API |

##### snapshot を確認削減の根拠にしない

**snapshot は日常の取り消し機能**であって保全策ではない。次の 3 つが理由。

| 限界 | 内容 |
| --- | --- |
| **復旧データ自身が破壊できる** | 保存先は `allowWrite` 配下でなければ捕捉できず、**同じ shell から消せる**。作業と復旧データを一度に失いうる |
| 捕捉は best effort | 公式仕様。失敗しても会話は続くので**気づけない** |
| 復元が上書きしうる | 後から人間が加えた編集を潰すことがある |

対象外も残る。

- `.gitignore` されたファイル、2 MiB 超の未追跡ファイル
- 作業ディレクトリの外
- ファイル以外の副作用（プロセス・DB・ネットワーク）

##### 独立した復旧元は「信頼済みリモートへ push 済み」のもの

**クリーン判定は保全ではない。** 同じ shell から `.git` のオブジェクトや参照を
消せるので、**コミット済みの履歴もローカルでは失いうる**。ignored ファイルの
状態もクリーン判定では見えない。

| 対象 | ローカルの破壊で失うか | 備考 |
| --- | --- | --- |
| **信頼済みリモートへ push 済み** | **ローカルの破壊だけでは失われない** | **復旧にはリモート側の保持と可用性が要る** |
| ローカルの履歴（未 push） | **失いうる** | `.git` は `allowWrite` の中 |
| 未コミット・未追跡 | **失いうる** | 同上 |
| ignored ファイル | **失いうる** | snapshot の対象外でもある |

**この喪失を受容する。** 起動時のゲートは保全ではなく
**「未保存の変更を持ち込んでいないか」の確認**と位置付ける。

| 状態 | 挙動 |
| --- | --- |
| クリーン | そのまま起動 |
| **未コミット / 未追跡あり** | **中止**。コミット（できれば push）するか、明示的に承知して起動 |

##### 「push 済みなら安全」と言い切らない

境界内からの `git push` と `gh` の書き込みは失敗する（実測）。
確認できているのは次の範囲にとどまる。

- 指定した資格情報ファイルを内側から読めない
- **試した** `git push` と `gh` の書き込みが失敗した
- 確認したトークンは `read:user` の権限しか持たない

**これはリモートを変更する経路がすべて無いことの証明ではない。**
環境変数のトークン・SSH 鍵と agent socket・別の credential helper・
認証済みサービスの有無は、この根拠からは分からない。ある push の失敗は、
同じ資格情報による別の ref 操作や API 経由の変更まで否定しない。
リモート側の喪失（他者による削除・履歴改変）も別の話。

##### 復旧先と基準を境界の外に固定する

`.git` を変更できるなら、**remote の URL も remote-tracking ref も変更できる**。
`origin` と呼ぶだけでは復旧先がすり替わりうる。

| 境界の外に記録するもの |
| --- |
| 信頼するリモートの識別情報（URL） |
| **push 済みであることを確認した基準コミット ID** |

コミット ID の記録は履歴そのものの保全ではないが、
**復旧先や基準のすり替えを防ぐために要る**。

> **`worktree` を切ることは保全ではない。** 新しい worktree は元の未コミット
> 変更を自動保存せず、Git の共通メタデータも共有する（外部レビューの指摘）。

**確実にしたいなら「原本を書き込み範囲外に置き、破棄可能な作業コピーだけを
渡す」形にする。** 現時点では採らず、上記の喪失を受容する。

> 誤った `stage` を `commit` すると**セッションが切り詰められ、復元機会が
> 消える**（実地で壊した）。`/undo` は取り消せるが `commit` は戻せない。

#### `policies` は運用上の禁止だけ

グローバル設定に置くと**プロジェクト設定から解除できない**。

| 置くもの | 例 |
| --- | --- |
| 運用上の禁止 | `shell:git push *`、`shell:gh repo delete *` |
| 人間専用と決めた展開操作 | `shell:chezmoi apply *` |
| 使う provider 以外 | `provider.use` を全体 deny → 使うものだけ allow |
| 同じ作用を持つ MCP ツール | `git push` だけ禁じて MCP が通るのは不統一 |

**任意コードに対する境界ではない。** permission 検査を通る操作にしか効かず、
plugin のコードを sandbox しない。不正な statement は**警告付きで破棄される**
ので、置いた事実ではなく**有効性**を確認する。

`external_directory:*` の全面 deny は、意図的に開けた `/mnt` の参照先も
止めうるので、許可パスと整合させる。

#### 境界をエージェントへ伝える

**隠すことと存在しないことをエージェントは区別できない。** 実測では
`ls ~/.ssh` の `ENOENT` を見て「存在しないので作れます」と誤診した。
実行していれば鍵は消える領域へ書かれていた。

システムプロンプトで伝える（**説明上の対策であって、書き込み失敗を
保証するものではない**）。

1. ホスト HOME の大部分は隠されている。`ENOENT` はホスト上の不存在を意味しない
2. 成果の永続化先は指定のワークスペース。他の永続化先・読み取り専用の例外は
   明示されたものに限る
3. 一時領域への書き込みはホストの同名パスへの反映を意味せず、隔離終了時に破棄される
4. 鍵生成・認証設定・ホストへの展開は境界の外で行う
5. 内側の観測だけでホストへの反映を報告しない
6. 拒否されたアクセスを、別パス・symlink・別ツールで回避しない
7. ホストで実行されるコードを変更したら、対象と**実行される契機**を報告する

**完了条件**: 225 件すべてに処遇が割り当たり取りこぼしが 0 であること。
隔離版の緩和が通常版に波及しないこと。snapshot の保存先が永続領域にあること。

#### 完了の確認（2026-09-23）

実装は「隔離版を別に列挙する」のではなく、**通常版の宣言から導出する**形に
なった。`build_opencode_sandbox_permissions()` が `drop_shell`（正規表現 14 本）と
`drop_path_prefixes`（7 本）で通常版を篩う。宣言を足せば両方へ反映されるので、
**取りこぼしは構造的に起きない**（列挙し直す設計なら起きる）。

| 確認 | 結果 |
| --- | --- |
| 通常版 | **225 件**（deny 193 / ask 27 / allow 5）。変わっていない |
| 隔離版 | **168 件**（deny 150 / ask 17 / allow 1） |
| `policies` | 4 件（`git push` / `gh repo delete` / `chezmoi apply` / `chezmoi re-add`） |
| 設定系統の分離 | `config_dir` = `~/.config/opencode-sandbox`。`ocs` が `OPENCODE_CONFIG_DIR` で指す |
| `data_home` / `db` | 起動ディレクトリ配下（`.opencode-sandbox/`）。永続領域にある |
| システムプロンプト | `[opencode.sandbox] system_prompt` の 7 項目を `config_dir/AGENTS.md` へ書き出す |
| 試験 | `test/agents/test_generate_opencode_sandbox.py` **21 passed** |

着地は 168 件で、見込み（130〜150 件）より多い。`shell` の既定反転で消えるのは
`shell:*` 1 件だけで、**境界が守らないワークスペース相対の規則を捨てなかった**ため。

##### 残っているもの: 強さの列を持つ対応表

**唯一の未完。** 3 分類（OS でも強制 / ツール操作上の禁止のみ / 運用）の定義と
注意書きは `common.toml.tmpl` の `[opencode.sandbox.permissions]` にあるが、
**各規則が持つ列にはなっていない**。フィルタ規則を束ねるコメントとして
書かれているだけで、元 225 件との 1:1 の対応は追えない。

「残したから守られている」と読まれる危険は、この対応表が無い限り残る。

### 段階 4: 許可リストを `common.toml` で単一ソース化（完了）

パスとドメインの両方を `common.toml` から生成する。

#### 起動ディレクトリを境界にする

**ワークスペースは起動ディレクトリ。** その配下は無条件に読み書きできる。
どこで起動するかは**利用者の責務**とし、宣言の無い場所でも起動できる。

```toml
# <起動ディレクトリ>/.opencode/sandbox.toml
read = ["/mnt/d/datasets/example"]
write = ["/mnt/d/outputs/example"]
network_allow = ["api.example.com"]
```

| 挙動 | 内容 |
| --- | --- |
| ワークスペース | 起動ディレクトリ（`cwd`） |
| 追加の許可 | **要るときだけ**要求する。共通分への上乗せ |
| 要求の置き場 | `<起動ディレクトリ>/.opencode/sandbox.toml` |
| 当たり方 | **その場のものだけ**。親の要求は継がない |
| 要求が無い場所 | 起動できる。追加はゼロ |
| 非 Git リポジトリ | 起動できるが、**snapshot が効かない旨を警告**する |
| `data_home` / `db` | 起動ディレクトリごとに分かれる |

#### 要求と承認を分ける

**リポジトリは「要求」できるが「付与」はできない。**

```text
<プロジェクト>/.opencode/sandbox.toml         ← 要求（リポジトリと一緒に移動）
~/.local/state/opencode-sandbox/trusted.json  ← 承認（マシンごと・境界の外）
```

| 場面 | 動き |
| --- | --- |
| 初回 | 要求内容を表示し、**承認するまで起動しない** |
| 承認後 | 記録に残り、次回から黙って通る |
| 要求が変わった | ハッシュが変わるので**再承認** |
| 対話できない | **起動しない**（`--trust` を付けたときだけ通す） |

承認画面には、`~` や相対パスを**展開した後の形**を出す。人が承認するのは
「何が開くか」であって、書かれた文字列ではない。

```console
$ ocs
このリポジトリは境界の外への追加の許可を要求しています。
  /home/u/src/example/.opencode/sandbox.toml

  読める : /mnt/d/datasets/example
  書ける : (なし)
  通信  : api.example.com

  起動ディレクトリ (/home/u/src/example) 以下は元から読み書きできます。
  ここに挙がっているのは、それ以外に開くものです。

許可しますか? [y/N]:
```

> **なぜ承認が要るか。** 要求だけで通すと、敵対的なリポジトリが
> `~/.ssh` を自分で開けられる。これは**仮定ではなく実在する穴**で、
> OpenCode 自身の permission がまさにそうなっている
> （[permission の穴 §5](../research/opencode/permission/gaps.md#5-プロジェクト設定がグローバルの-deny-を上書きする2026-09-23-再確認)）。
>
> 承認の記録は**境界の外**（`~/.local/state`）に置く。内側から書けるなら
> エージェントが自分で自分を承認できる。
>
> 要求ファイルは `.opencode` 配下なので `denyWrite` で保護されている。
> ただしそこに寄りかからない。**保護が外れても、変更はハッシュで検出して
> 再承認になる。**
>
> **複数 PC で持ち回れる。** 要求がリポジトリと一緒に移動し、パスが
> マシン依存にならない。承認だけマシンごとに要る。

#### プロジェクトごとのパス許可

chezmoi 管理下でも機密でないパス（`~/.config/shell` など）は、
プロジェクト単位で開けられることを実測した。

| パス | 設定 | 結果 |
| --- | --- | --- |
| `~/.ssh` / `~/.config/gh` / `~/.config/opencode` | 未許可 | **拒否** |
| `~/.config/shell` / `~/.config/mise` | `allowRead` | **読める** |
| `. ~/.config/shell/shellenv.sh` | — | **読み込み成功** |

`/mnt` 配下も狙い撃ちで開けられ、**symlink 越しにも届く**
（[調査記録 25 節](../research/opencode/permission/sandbox-runtime.md)）。
データ処理で `/mnt/d` の対象を扱う場合はここに書く。

**完了条件**: プロジェクトを cwd で選び、宣言の無い場所では起動しないこと。

#### ドメイン

`[sandbox] shell_network_allow` は現在 **Claude 専用**（Copilot にドメイン単位の
制御が無いため）。OpenCode でも使えば同じリストを共有できる。

実履歴で不足が判明しているものを先に足す。

| 追加するもの | 根拠 |
| --- | --- |
| `opencode.ai` | 許可外で 50 件が失敗する見込み |
| `claude.ai` | 同 3 件 |
| `mise.jdx.dev` | 実地で警告が出た（`mise-versions.jdx.dev` はある） |

不足しても破綻はせず接続が失敗するだけなので、追記で回復できる。

##### 開発ツールのために許可ドメインを増やさない（2026-09-23 に判断）

境界内で `mise exec` を叩いたら、未インストールのツール 18 個を入れに行き、
**166 秒ハングした末に失敗した**。「許可ドメインが足りない」と読みたくなるが、
**内訳を見ると違う**。

| ドメイン | 状態 | 用途 |
| --- | --- | --- |
| `github.com` / `objects.githubusercontent.com` | **許可済み・成功** | uv / gh / sops / hadolint の本体取得 |
| `api.github.com` | **許可済み・403** | rate limit。境界内に資格情報が無い帰結 |
| `tuf-repo-cdn.sigstore.dev` | 未許可 | mise の sigstore 検証 |
| `tmaproduction.blob.core.windows.net` | 未許可 | GitHub artifact attestation の実体 |
| `www.python.org` | 未許可 | python のソースビルド |

**本体の取得は成功していて、落ちたのは署名検証と rate limit。**
`mise.jdx.dev` / `pypi.org` / `files.pythonhosted.org` は既に許可済みだった。

したがって**増やさない**。理由は 4 つ。

| 理由 |
| --- |
| 原因がドメイン不足ではない。**境界内はビルド環境ではない** |
| 検証を通すには `*.blob.core.windows.net`（Azure Blob の汎用ホスト）が要る。**任意アカウントのストレージへ届き、持ち出し経路として広すぎる** |
| 中途半端に開けると「検証を無効化する」方向へ圧力が掛かり、供給網の保護が落ちる |
| `api.github.com` の rate limit はドメインでは解決しない |

> **ドメイン許可は送信内容も相手アカウントも制限しない。**
> 「取得に使う」つもりで開けた先は、そのまま送信先にもなる。

代わりに**境界内では mise の自動インストールを止め、即座に失敗させる**。
ツールはホスト側で `mise install` しておけば、境界内は解決だけで済む。

| 設定（`[opencode.sandbox.inner_env]`） | 効果 |
| --- | --- |
| `MISE_AUTO_INSTALL` / `MISE_EXEC_AUTO_INSTALL` / `MISE_NOT_FOUND_AUTO_INSTALL` = `false` | 黙って入れに行かない |
| `MISE_OFFLINE=1` | 版の解決でネットワークへ出ない |
| `MISE_LOG_LEVEL=error` | 上記で出る警告 30 行を抑える。**エラーと終了コードは残る** |

実測で **166 秒 → 0.2 秒**（`rc=0`）になり、未インストールのツールを呼ぶと
`rc=1` で落ちる（判別可能な失敗になった）。設定名は `mise settings --all` で
実在を確認し、環境変数が効くことを `mise settings get` で確かめた
（`mise 2026.9.11`）。

> **この変更は境界の内側からは入れられなかった。** 対象の
> `common.toml.tmpl`・`generate.py`・`ocs` は全て `protected` の
> `denyWrite` 配下で、書き込みが `EROFS` になる。**保護機構の自己改変を
> 阻止する設計が働いた形**なので、適用は境界の外で人が行う。

#### GitHub の認証情報は当面入れない

**境界内には追加の資格情報を置かない。** 実履歴では 25 件がこれで失敗する
見込みだが、必要が生じるまで持ち込まない。

##### すでに境界内にあるもの

「認証情報を一切入れない」は**不正確**。モデルの OAuth は DB にあり、
**境界内の任意コードから読める**ものとして扱う。実測した権限は次のとおり。

| 項目 | 実測 |
| --- | --- |
| 種別 | `github-copilot` の OAuth（`gho_`、refresh トークンあり） |
| **GitHub での権限** | **`read:user` のみ**（`X-OAuth-Scopes`） |

`gho_` は GitHub の OAuth トークンなので「モデル専用だから他に使えない」とは
**推定できない**。実際に `api.github.com` へ当てて範囲を確かめた。
**リポジトリの読み書きも `git push` もできない。** 残るリスクはモデル推論の
実行（課金・枠）とプロフィールの読み取り。

> **provider を替えたら測り直す。** 別のトークンが入る。

##### 棚卸しの対象（2 ファイルだけではない）

| 経路 | 扱い |
| --- | --- |
| `~/.git-credentials` / `~/.config/gh/hosts.yml` | `denyRead` で不可視（実測で `git push` / `gh` 書き込みが止まる） |
| 継承した `GH_TOKEN` / `GITHUB_TOKEN` 等 | ランチャーが**環境変数を絞って**起動する |
| SSH agent / credential-cache の socket | `denyRead` と seccomp。起動時チェックで確認 |
| Git の remote URL に埋まった資格情報 | ワークスペース内なので境界外の話ではない |
| DB 内の OAuth・MCP 認証情報 | 上記のとおり境界内にある |

##### 必要になったときの選択肢

当面は**境界の外で必要な取得を済ませ、内側には取得済みのデータを渡す**のが
最も単純（外部レビューの助言）。それでも足りない場合の候補は、対象を絞った
fine-grained PAT、読み取り専用 deploy key、権限を絞った GitHub App の
短寿命トークン。いずれも組織ポリシー次第。

**classic PAT は使わない。** 読み取り専用にできないため。

**完了条件**: 検証コマンド（`uv run` / `pre-commit` / `mise`）と `gh` の参照・
`git fetch` が境界内で通ること。失敗で止まった件数を記録し、許可リストへ
追記していく運用が回ること。

### 段階 5: 境界内で無意味になった層だけを削る（改訂）

> **当初案を撤回した（2026-09-23）。** 「`grep` / `glob` を無効化して
> ファイル参照を shell へ寄せる」「誘導を削る」は、**実施すると保護が下がる**。

#### 撤回の理由: 前提が成り立たなかった

当初はこう書いていた。

| 経路 | いま | 境界の導入後（当時の想定） |
| --- | --- | --- |
| shell | 保護が弱い | **OS が強制する** |
| `grep` / `glob` | 結果フィルタ（事後の除去） | 変わらず弱い |

**「OS が強制する」はホストのパスにしか当てはまらない。**
ワークスペースの中は境界が守らないので、shell からは `.env` も `*secret*` も
読めるし `.git/hooks` も書ける（[段階 3](#段階-3-隔離版の設定系統を分け規則を作り替える概ね完了)）。

したがって当初案を実施すると次が起きる。

| 変更 | 結果 |
| --- | --- |
| `grep` / `glob` を無効化 | 検索が shell へ移る |
| 結果フィルタが消える | **ワークスペース内の秘密が素通りする** |
| 誘導（`cat` → `read`）を削除 | 読み書きが shell へ移り、**`read` / `edit` の deny 108 件が空振りする** |

**残した 108 件の「ツール操作上の禁止」は、読み書きがツール経由になって
初めて機能する。** 誘導はその前提を作る層なので、消すと規則だけが残る。

#### 層ごとの判断（改訂）

| 層 | 件数 | 判断 | 理由 |
| --- | --- | --- | --- |
| 誘導（`cd`） | 1 | **残す** | 作法。境界と無関係 |
| 誘導（`cat` / `head` / `tail`、`sed -n`） | 2 | **残す** | **ワークスペース内の秘密に対して有効**。読み取りを `read` へ寄せて deny を効かせる |
| 誘導（`cat >` / `tee <<`、ヒアドキュメント） | 2 | **残す** | `.git/hooks` の `edit` deny を効かせるために要る |
| 結果フィルタ（`grep` / `glob`） | 37 | **残す** | `grep` / `glob` を無効化しないので引き続き要る |
| 伏字化: 内容の形 | 9 | **残す** | 境界内の `.env` などに効く |
| **伏字化: パス参照** | 34 | **不活性**（下記） | 対象がホストのパスで、境界内から到達できない |
| `tools` による `grep` / `glob` の無効化 | — | **採らない** | 事後の除去より弱い経路へ寄せることになる |
| permission（隔離版） | 168 | 維持 | 段階 3 のとおり |

#### パス参照の伏字化 34 件は消さない

唯一「境界内で無意味」と言えるのがこれだが、**消さずに残す**。

| 判断 | 理由 |
| --- | --- |
| 消さない | 不活性なだけで害が無い。削除には plugin へ分岐を足す必要があり、複雑さが増える |
| 通常版では現役 | 同じ宣言を共有している。隔離版のためだけに分岐させる価値が無い |

**「規則が減ること」は成果ではない。** 減らすために分岐を足せば、
保守する対象は逆に増える。

#### この段階で得られるもの

削減はゼロ。得られるのは**判断の記録**だけになる。

- `grep` / `glob` の無効化は**採らない**と確定した
- 誘導 5 件は境界内でも全て有効だと確定した
- 段階 3 で読み込んだ `guide-plugin` は、**削らずそのまま使う**

> [CHG-0002](0002-opencode-ask-by-default.md) の保留分も、この判断に合わせて
> 見直す。「境界ができれば不要になる」としていたものの一部は、
> **ワークスペース内では不要にならない**。

**完了条件**: 上記の判断が記録され、`grep` / `glob` が有効なままであること。

## 着手順（2026-09-23 に確定）

外部レビューで洗い出した項目に、[CHG-0005](0005-agents-config-naming.md)（設定の
命名整理）を交えた全体の順序。**A 系統と B 系統に依存関係は無い。**

```text
B1  →  A1  →  A2  →  B3  →  B2  →  B4  →  A4 / B5 を再評価
```

| 段 | 内容 | 案件 | 規模 | 危険度 |
| --- | --- | --- | --- | --- |
| **B1** | `cd ~ && ocs` で境界が消える穴を塞ぐ | CHG-0004 | 数行 | **完了**（2026-09-23） |
| A1 | `common.toml` の改名 | CHG-0005 | 62 箇所 | **完了**（2026-09-23） |
| A2 | 固有設定 57 件を native へ分離 | CHG-0005 | 57 件 | 保留（CHG-0006 待ち） |
| **B3** | TOCTOU・fail-open・保護チェックの欠陥 | CHG-0004 | 小〜中 | **完了**（2026-09-24。#4 も対処） |
| **B2** | `seed_db` のホスト DB 複製と資格情報の扱い | CHG-0004 | 中 | **DB は完了**（2026-09-24）。資格情報は保留 |
| B4 | セッション引き継ぎ（`export` / `import`） | CHG-0004 | 中 | **完了**（2026-09-24） |
| A4 / B5 | 廃止の是非 / 静的境界 | 両方 | — | 保留 |

順序の理由:

| 位置 | 理由 |
| --- | --- |
| **B1 が最初** | 実在する穴で、タイプミス 1 回で発火する。数行で塞げる |
| A1 が 2 番目 | 挙動が変わらず、**`chezmoi diff` が空**という完全な合格条件がある。最も安全。しかも A2 の範囲が名前だけで自動的に決まる |
| B3 → B2 の順 | B3 は自己完結。B2 は**境界内からモデルへ到達できるかの実機確認**が要り、止まりうる |
| A4 / B5 が最後 | 今決める材料が無い。A1〜A3 後に `generate.py` がどれだけ軽くなるか見てから |

**未確定**: 「完成」（＝再び `opencode` の既定を `ocs` にしてよい条件）は
未定義のまま。少なくとも B1〜B4 が前提になる。

## 外部レビュー（2026-09-23）: 未対処の欠陥と簡素化の選択肢

段階 6 の後、`github-copilot/gpt-6-astra` へレビューを依頼して洗い出した。
**この節は記録であって、方針の決定ではない。** 着手順は未定。

### 最も重い発見: 起動ディレクトリの選び方で境界が消える

> **2026-09-23 に B1 として対処した。** 判定を `deny_read` から導き、
> 打ち消す場所での起動を拒否する（`reject_unsafe_workspace`）。以下は
> 対処前の観測記録。

`cd ~ && ocs` を実測した。起動ディレクトリは無条件に `allowRead` と
`allowWrite` へ入り、**`allowRead` は `denyRead` に勝つ**（R3）。したがって
ホームで起動すると `denyRead: ~` が打ち消される。

| 内側からの観測（境界を張って実測） | 結果 |
| --- | --- |
| `~/.ssh` / `~/.config/gh/hosts.yml` / `~/.git-credentials` | **READABLE** |
| `ls ~/.ssh` | `authorized_keys` `aws` `aws.pub` … **一覧できた** |
| ホーム直下への書き込み | **書けた** |

**設計どおりの動作**（「どこで起動するかは利用者の責務」）だが、
**`cd` のし忘れ 1 回で保護がゼロになる**ということでもある。
新しいシェルタブ、`cd -` の勘違い、タイプミスで起きる。

対処の候補（未決）:

> **採用したのは「危険な場所の拒否」。** ただし判定はハードコードせず
> `deny_read` から導く形にした（打ち消す項目そのものと、その祖先を拒否）。
> ハイブリッド登録と静的境界は引き続き保留。

| 案 | 内容 | 得るもの / 失うもの |
| --- | --- | --- |
| **危険な場所の拒否** | `~` とその直下などでの起動を断る | 数行で済む / 意図して使う場合も塞がる |
| **ハイブリッド登録** | 未登録の場所では初回に範囲を見せて確認し、承認したら記録 | 事故を防ぎ、任意の場所も使える / **コードは増える。簡素化にはならない** |
| **静的境界** | 登録済みの場所でしか起動しない | 簡素化できる / 任意の場所で即起動できない |

### コードで裏が取れた欠陥（5 件）

> **2026-09-24 に B3 として #1・#3・#4・#5 を対処した。** #2 は DB の作り方を
> 直したが、**資格情報の扱いは別案件として保留**（方針の判断と実機確認が要る）。

| # | 欠陥 | 根拠 | 状態 |
| --- | --- | --- | --- |
| 1 | 境界設定の一時ファイルが `TMPDIR` 任せで**ワークスペース内（`allowWrite` 領域）**に作られ、`execve` のため消えない | 残骸を実測（`.tmp/` に 18、`/tmp/` に 1）。同一ワークスペースで別セッションが動いていれば、srt が読む前に改ざんしうる | **対処済** |
| 2 | `seed_db` が**ホスト DB 全体をワークスペース内へ複製してから**不要テーブルを削除 | 削除前の全セッションが読める時間帯がある。途中で落ちると `db.exists()` で初期化を飛ばし未削除 DB を再利用 | **対処済**（資格情報は保留） |
| 3 | 境界チェックのスクリプトが無いと**検査せず起動**（fail-open） | `if not args.skip_check and CHECK.is_file():` | **対処済** |
| 4 | 承認した要求と適用する要求が**別々に読み込まれる** | `read_request()` を `ensure_trusted` と `build_boundary` が個別に呼ぶ | **対処済** |
| 5 | 保護対象が「書ける通常ファイル」のとき**チェックが合格する** | `-d` でなければ `mkdir -p` の失敗を合格扱いにする | **対処済** |

#### B3 の対処内容（2026-09-24）

| # | 変更 |
| --- | --- |
| 3 | `CHECK` が無ければ `die()`。「境界を張れないなら起動しない」方針とここだけ逆向きだった |
| 5 | `-e` の分岐を足し、**追記（`>>`）で書き込み可否を見る**。`>` は保護対象を切り詰めるので使わない |
| 1 | `~/.local/state/opencode-sandbox/boundaries/`（`0700`）へ移した。`execve` で消せないぶんは `prune_boundaries()` が次回起動時に 24 時間より古いものを捨てる。**稼働中のセッションの分は残す** |
| 4 | 要求を `main()` で**一度だけ**読み、同じ辞書を `ensure_trusted` と `build_boundary` へ渡す |
| 2 | **スキーマだけ写し、`DB_KEEP` の行だけを入れる**方式へ。ホストの会話は一度もワークスペースに置かれない。書き途中は `.building` に作って `rename` する |

`0600` では足りない理由は、**同一 UID では別セッションを隔離できない**から
（外部レビューの指摘）。パーミッションは他人への防御で、自分の別プロセスには
効かない。置き場所を `deny_read` の内側へ移すことで、内側から見えなくした。

#### #2 の実測（対処前後）

実物のホスト DB で試した。

```text
変更前: 11,336 KB  ← 全部写してから削除。途中で落ちると未削除のまま再利用
変更後:    240 KB  ← credential 1 行 + migration 47 行のみ
```

スキーマ 20 テーブルは残す（消すと OpenCode が作り直せない）。
削除方式をやめたので、SQLite の空きページに本文が残る問題も消えた。

#### #4 を「消さずに直した」理由

外部レビューは「要求と承認の責務を削除できる」と挙げていたが、**残した**。

- 修正は 1 行（読んだ辞書を渡すだけ）で、消すより安い
- `/mnt/d` のデータセットを狙い撃ちで開ける手段は、**機械学習の作業を境界内で
  やるなら要る**。今使っていないだけで、消すとそのとき作り直しになる

### 記録されていなかった事実: 資格情報は境界内にある

`DB_KEEP` が `credential` を引き継ぐため、隔離 DB はモデル API の資格情報を
持つ。その DB は**ワークスペース内**にあり、内側から読める。

```text
隔離 DB が保持している資格情報: ['github-copilot']
.opencode-sandbox/opencode.db はワークスペース内: True
```

**「ホストの秘密を境界内へ出さない」は文字どおりには成立していない。**
OpenCode の稼働に必要なものを明示的な例外とするか、決めていない。

### 簡素化の選択肢

現状は **881 行 / 35 関数 / 7 責務**。レビューの診断は
「行数より、**毎回ホストで環境を準備し直す**設計が複雑さの中心」。

| 責務 | 削れるか |
| --- | --- |
| 境界の組み立て | 静的化すれば縮小できる |
| 要求と承認 | **削除できる**（実測: このマシンで未使用。`trusted.json` も未作成） |
| 作業ツリーの退避 | 境界外のバックアップへ委譲できる。無条件には削れない |
| 隔離版設定の生成 | `chezmoi apply` 時へ移せる（実測: 引き継いでいる値は現在ゼロ） |
| 隔離用 DB | 分離は残し、**ホスト DB の複製をやめる** |
| 境界チェック | キャッシュを削って毎回必須にする方が単純 |
| 起動 | 残す |

**セッション引き継ぎは DB 共有を必要としない。** `session import` が ID を
保つ（実測済み）ので、必要なものだけ移送すれば足りる。隔離セッションを
ホスト DB へ移せば、OpenCode が終了時に表示する `opencode -s <ID>` が
**そのまま動く**。これにより `execve` → spawn + wait の変更は不要になる。

### プロジェクト一覧の置き場

静的境界にする場合、一覧を `common.toml` に書くと
**PUBLIC リポジトリへ push される**（`applejxd/dotfiles` は公開）。

`~/.config/agents/local.toml` が既にその用途で用意されている。

- chezmoi 管理外で **push されない**。`apply` でも消えない
- **追記のみ**で、共有設定の `deny` を弱められない
- `~/.config/agents` は境界内から**不可視**（実測）。エージェントは一覧を
  読むことも、書き足して自分の権限を広げることもできない

ただし現状 `LOCAL_SANDBOX_KEYS` は Claude / Copilot 用のキーしか受け付けず、
**OpenCode 用の受け口は無い**。

### 未検証の指摘

次はもっともらしいが、この環境では確かめていない。

- `git add` が `.gitattributes` の filter を実行しうる（＝境界を張る前に
  ホストでコードが走る）
- `NODE_OPTIONS` / `LD_PRELOAD` 等が境界構築前の実行に影響しうる
- srt が子の SIGINT/SIGTERM を `0` に変換している可能性（upstream のコード）

## 評価基準

**必須:**

1. Claude / Copilot / Codex / Gemini の生成物に diff が出ないこと
2. **保護対象 × 禁止操作 × 実行経路の組み合わせごとに、禁止操作を阻止する機構と
   検証根拠があること。** 阻止できない組み合わせは残余リスクとして明示し、
   受容を判断すること
3. **境界が起動していないとき、確認が減らないこと**（fail-closed）
4. **包めないときは実行を止めること。** 通常の shell 実行へ落ちるなら境界ではない
5. `chezmoi apply` を実行する手順が残っていること
6. 短いコマンドの実測遅延が、先に決めた許容値以内であること

**基準 2 の数え方。** 読み取り・書き込み・外部送信を分けて見る。
**結果フィルタは「実証できた情報開示の抑制」だけを担保として数え、
アクセス拒否とは同列に扱わない**（件数・エラー・集約に情報が残りうるため）。

> **削減目標は未達。** 当初の必須基準「規則と除外条件の総数が減っていること」は
> 達成できない。組み込みツールが境界の外に残り、それを守る規則を捨てられない
> ため。**安全性の改善を優先して必須条件を差し替えた。**
> 総数は補助指標として追い続ける（現在 310 件）。
>
> 「Claude Code も同じ構造だから健全」という論拠も**撤回した**。実装上の前例では
> あるが、残余リスクを受容してよい根拠にはならない（外部レビューの指摘）。

## 候補比較

| 候補 | 支持する根拠 | 不利な点 | 扱い |
| --- | --- | --- | --- |
| **`srt` で shell の実行ごとに包む** | 汎用・導入済み・proxy と seccomp が同梱。実地で成立 | 組み込みツールが外に残る。固定費 | **採用** |
| サービスごと包む | 組み込みツールも覆える | **不成立**。ネットワーク名前空間の除去で TUI から届かない。`--stdio` も不可 | **見送り** |
| **`grep` / `glob` を `tools` で無効化する** | 事後の除去に頼る経路を消せる。**設定 1 行で済み、モデルに提示すらされない**（plugin 不要） | `grep` は最多の呼び出し（114 件）で固定費が乗る（合計 約 4.6 秒） | **採用**（段階 5） |
| `grep` / `glob` を plugin の例外で止める | 同じ効果。実測で成立 | ツール定義のコンテキストを消費し、失敗の 1 往復が生じる | **見送り**（`tools` が上位互換） |
| `permissions` 配列で `grep` を deny | 既存の仕組みで済む | **効かない。** V2 の配列は `grep` / `glob` をアクションとして持たず黙って無視する | **不可** |
| `read` / `edit` も無効化して shell へ寄せる | 全ファイル操作が境界の内側になる | `edit` に確実な代替が無い。信頼性と確認負担が悪化する | **見送り** |
| bwrap を自作でラップ | 依存が減る | proxy と seccomp を自作することになる | **見送り** |
| Landlock | mount namespace を作らない | ファイル制限だけで通信・IPC を覆えない | **見送り** |
| 列挙型を続ける | 追加費用ゼロ | 書き込み保護が成立せず、規則と誤爆が単調増加 | **打ち切り** |
| `gh` を文字列で fetch のみ許可 | 実装が小さい | `gh api -X POST` で抜ける | **見送り**。認証情報を渡さないことで代替 |

## 保留にしたもの

[CHG-0002](0002-opencode-ask-by-default.md) 段階 4 以降のうち、次は**保留**する。
境界ができれば不要になるか、作り直しになるため。

| 保留するもの | 理由 |
| --- | --- |
| リダイレクト先を分解して `edit` の判定表に当てる | **ホストについては**境界が同じことを OS 側で行う。ワークスペース内には効かないので、必要になったら戻す |
| `write_deny_globs` にシェル起動ファイルを追加 | 境界の内側に入れなければ足りる（macOS / Windows では別途必要） |
| 少数の静的 allow を増やす | 隔離版では既定が `allow` になったので不要 |

> **「境界ができれば不要になる」としたものの一部は、ワークスペース内では
> 不要にならない**（段階 5 の改訂）。保留の理由を書き直した。

## 次の調査・実験

| # | 減らしたい不確実性 | 方法 |
| --- | --- | --- |
| P1-1 | 異常時の挙動（並行実行・キャンセル・異常終了） | 実装時に確認する |
| P1-2 | 「境界内で書けずに失敗する」コマンドの洗い出し | 実運用で失敗件数を記録し、許可パスへ追記する |
| P1-3 | 境界の外のサービス・ソケット経由の代理実行 | proxy 経由の到達先・継承 FD・リモート MCP を洗う |
| P1-4 | **`docker` / `aws` 等の到達性** | socket・リモート設定・環境変数を確認してから deny を外す |
| P1-5 | **審査前のホスト実行経路** | mise タスク・Git hooks・検証スクリプトが外側で動く契機を洗う |
| P1-6 | 隔離版の実効設定が本当に分かれているか | 版ごとに設定・policy・DB を突き合わせる |

### 解決済み

| 番号 | 問い | 結果 |
| --- | --- | --- |
| P1-8 | 宣言をプロジェクト側へ置けるか（複数 PC 対応） | **置いた**。要求と承認を分けた（[段階 4](#要求と承認を分ける)） |
| P1-7 | プロジェクト設定が隔離版の permission を緩められるか | **緩められる**（2026-09-23 / `v2.0.12`、TUI とヘッドレスで実測）。下記 |
| P1-9 | セッション中に境界を切り替えられるか | **できない**。下記 |

#### P1-7: プロジェクト設定は permission を緩められる（穴は実在する）

対照つきで再確認した。**2026-09-22 の記録どおり**で、プロジェクト側の
`permission` はグローバルの deny に勝つ。TUI・ヘッドレスとも同じ。

| プロジェクト側に置いたもの | 結果 |
| --- | --- |
| `permission` / `permissions` | **グローバルの deny に勝つ** |
| `experimental.policies` | **勝てない。グローバルが勝つ** |
| `.opencode/plugins/<名前>/index.js` | **実行される**（任意コード実行） |
| `mcp`（ローカルサーバ） | **プロセスが起動する** |

詳細は
[permission の穴 §5](../research/opencode/permission/gaps.md#5-プロジェクト設定がグローバルの-deny-を上書きする2026-09-23-再確認)。

**この案件の前提は変わらない。** `policies` は保たれるが、plugin と mcp を
止められないので単独では足りず、**プロセスごと隔離する**必要がある。

> **測り方で 4 回踏んだ。記録しておく。**
>
> - **`PWD` を合わせる。** 子プロセスの `cwd` だけ変えても OpenCode は `PWD` を
>   見てプロジェクトを決める。これを外したまま 10 回以上測り、
>   「プロジェクト設定は何も効かない」という**誤った結論を一度コミットした**。
>   見分け方は、相対パスでファイルを作らせてどこに出来るかを見ること
> - **「読み込まれている」を「効いている」の根拠にしない。** `/api/config` には
>   試験用ワークスペースの設定が並んでいたが、効いていなかった
> - **判定語をコマンド文字列に入れない。** `echo pipmark-done` の成否を
>   出力の文字列一致で見たら、モデルが失敗報告にコマンド名を書いただけで
>   「成功」になった。**副作用（ファイルの有無）で判定する**
> - **条件を変えるときに対照を落とさない。** 設定ディレクトリを本物へ
>   戻した 3 回で対照を外し、「効かない」と「モデルが従わなかった」を
>   区別できない出力を 3 回続けて出した

#### P1-9: セッション中の切り替えはできない

| | Claude Code | この構成 |
| --- | --- | --- |
| 包む単位 | **コマンドごと** | **プロセス全体** |
| 切り替え | 実行時に可能 | **起動時に確定** |
| 覆う範囲 | shell のみ | shell・`read`/`edit`・plugin・LSP |

境界は bwrap の名前空間として**プロセス起動時**に作られ、以後変更できない。
ディレクトリを足したくなったら、終了 → 宣言を足す → 起動し直す。

**実行時に切り替えられること自体が案 B を不採用にした理由**でもある
（コマンド文字列で sandbox を解除する構造は認可として不適切）。
現実的な切り替えは起動時の選択（`opencode` / `ocs`）。

決着済み（[調査記録](../research/opencode/permission/sandbox-runtime.md)）:

- **P0-1 成立**。`opencode --standalone` ごと包める（TUI 含む実測）。
  「サービスごと包むと届かない」は**境界をまたぐ構成に限った話**だった
- **P0-2**。`chezmoi apply` は完全一致の例外では不十分。適用を人間の操作へ寄せる
- **P0-3**。版は `latest` の浮動指定で Claude Code と共有している
- **境界の外に残る経路**。`/mnt` と `/tmp` が開いていたので `denyRead` で塞いだ
- **境界内の資格情報の権限**。`read:user` のみ
- **snapshot が shell の `rm` を捕捉・復元できる**こと（`XDG_DATA_HOME` が条件）
- **起動時チェックの挙動ベース化と fail-closed**（`srt` は終了コードを伝える）
- プロジェクト単位のパス許可の粒度と、`/mnt` の狙い撃ち・symlink 越しの読み取り
- **保護機構そのものが書き換えられること**

## 段階 6: 利便性コストの棚卸しと是正（2026-09-23）

境界は成立したが、**Claude Code の sandbox と比べて利便性が落ちている**という
指摘を受けて全項目を洗い出した。構造の違いは次の 1 点に集約される。

| | Claude Code | この構成 |
| --- | --- | --- |
| 包む単位 | Bash コマンドごと | **プロセス全体** |
| read の既定 | **全計算機を許可**し deny で塞ぐ | **`~` ごと拒否**し allow で戻す |
| 境界外への逃げ道 | `dangerouslyDisableSandbox`（承認付き） | **無い** |
| 実行中の変更 | `/add-dir` が効く | 起動時に確定 |
| 履歴・設定 | 共有 | 分離 |

プロセス全体を包む選択は維持する（plugin / MCP / プロジェクト設定が permission を
上書きできるので、shell だけ包んでも守れない。[P1-7](#p1-7-プロジェクト設定は-permission-を緩められる穴は実在する)）。
**コストの多くはこの選択ではなく付随的な判断から来ていた。**

### 是正したもの（安全性を削らないもの）

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| **`ocs --continue` が素の起動になる** | `srt -c` はコマンド文字列を 1 個しか取らず、後続の引数を**エラーも出さずに捨てる** | 引数を文字列の中へ入れる（`inner_command`） |
| **skill が 1 つも読めない** | `~/.claude/skills` と `~/.agents/skills` が `deny_read: ~` に埋もれていた | `read` へ追加。read のみなので境界は弱まらない |
| **git worktree で `git status` すら落ちる** | linked worktree の `.git` は起動ディレクトリの外を指す | 共有 `.git` を `allowWrite` へ。**`hooks` と `config` は `denyWrite`**（ホストで実行されるコードを仕込めるため。Claude Code も同じ 2 つを閉じる） |
| 起動ごとに境界チェックの待ち（最大 180 秒） | 毎回フル実行していた | 判定に効く入力のハッシュで 24 時間だけ再利用。`--recheck` でやり直し |
| テーマ・キーバインドを選び直す | `config_dir` 分離の巻き添え | 通常版から**許可リストのキーだけ**引き継ぐ。既にある値は上書きしない |
| `ocs` と `opencode` の使い分け | ランチャーを分けた実装の都合 | `opencode` を関数にして既定を `ocs` にした。**2026-09-23 に撤回**（下記） |
| **未コミットで起動を断られる** | `require_clean` が gate になっていた | 起動前に境界の外へ退避し、gate は撤廃（下記 D'） |

実測（`srt` に同じ境界を張って内側から確認。対照を両方向で取った）:

| 確認 | 対処前 | 対処後 |
| --- | --- | --- |
| `~/.agents/skills` / `~/.claude/skills` | **hidden** | **VISIBLE**（`ls` で中身も取れる。書き込みは `EROFS`） |
| `~/.ssh` / `~/.config/gh`（対照） | hidden | hidden |
| worktree の `git status` | `fatal: not a git repository` | 正常 |
| worktree の `git commit` | 同上 | 成功 |
| 共有 `.git/hooks` / `.git/config` への書き込み | — | **`EROFS`** |

### 天秤にかけて保留したもの

| 項目 | 内容 | 保留の理由 |
| --- | --- | --- |
| **read の既定** | `denyRead: ~` をやめ、Claude 同様「広く read、秘密だけ deny」へ | **最大の脅威（秘密の読み出しと持ち出し）への主防御**。外すと `deny` リストの網羅性だけが頼りになる。中間案は作業用の親ディレクトリだけ read 許可 |
| **過去セッションの参照** | 境界内から通常版のセッションを read したい | 他プロジェクトの会話＝外部由来テキストの持ち込み。OpenCode に別 DB を read-only で覗く仕組みが無く、実装は `session export` / `import` の糖衣になる |
| コマンド単位の逃げ道 | `dangerouslyDisableSandbox` 相当 | プロセス単位で包む以上、原理的に作れない |

### `require_clean` を退避に置き換えた（採用: D'）

**warning 化は却下した。** `ocs` は警告を stderr へ出した直後に `execve` で TUI を
起動する。OpenCode のバイナリには代替画面の制御コード `?1049` があり、
**警告は TUI 起動と同時に消えてセッション終了まで戻らない**。止まらないうえに
読まれもしないので、保護と情報の両方を失う。

代わりに、起動のたびに作業ツリーを境界の外へ退避し、gate は撤廃した。

| | 旧 `require_clean` | 退避 |
| --- | --- | --- |
| 未コミットで起動 | 止まる（`--allow-dirty`） | そのまま起動 |
| 作業ツリー | 触らない | **触らない**（`git stash` とは別物） |
| 未追跡ファイル | git では戻せない | 保全（`.gitignore` 準拠） |
| 退避先 | — | `~/.local/state/opencode-sandbox/backups/` |
| コスト | 0 | **0.1 秒 / 1.9 MiB**（このリポジトリ、780 ファイル） |
| 大きすぎる場合 | — | 退避せず**起動も断る**（`--no-backup` で続行） |

退避先が成立する根拠（実測）:

| 確認 | 結果 |
| --- | --- |
| 境界内から `~/.local/state/opencode-sandbox` が見えるか | hidden |
| 内側で `mkdir` + 書き込み | 成功したように見える |
| **ホスト側に実在するか** | **存在しない＝幻の書き込み（R2）** |

副産物として、**承認記録 `trusted.json` の安全性も裏取りできた**
（内側の書き換えはホストへ届かない）。

設計上の要点:

- **同じ内容なら同じ tree SHA** になるので、変化が無い限り増えない。
  中断と再開を繰り返しても溜まらない（この懸念で D' を採った）
- **退避できなければ起動しない。** 「退避したつもり」で作業を始めるのが一番危ない
- tar として単体で展開でき、復旧に `.git` は要らない

#### 容量の上限（実測で決めた）

`du` ではなく「追跡 + 未追跡かつ非無視」の実サイズを測ったところ、
**手元のリポジトリは最大でも 54 MiB だが、1 つだけ 84 GiB のものがあった**。

| リポジトリ | 退避対象 |
| --- | --- |
| `~/src/dockers` | **84 GiB** |
| `~/src/marimo` | 54 MiB |
| `~/src/skill-lab` | 47 MiB |
| `~/.local/share/chezmoi` | 5 MiB（`du` では 650 MiB。差は `.gitignore`） |

当初の「10 世代 / 512 MiB」には 2 つの誤りがあった。

| 誤り | 直し方 |
| --- | --- |
| **作ってから間引いていた** | `git add` の前に測る。84 GiB を `.git` へハッシュしてから捨てることになる |
| **上限が起動ディレクトリごと**で、全体が青天井 | 全体の合計と期限を足す |

| 対象 | 上限 |
| --- | --- |
| 退避対象の作業ツリー | 256 MiB（超えたら退避せず起動も断る） |
| 起動ディレクトリごと | 5 世代 / 128 MiB |
| **全体** | **1 GiB** |
| 保持期間 | 30 日（触らなくなったプロジェクトの分を捨てる） |

実測: `~/src/dockers` で **0.09 秒で起動を断り、`.git` は無傷**（pack 66 KiB）。

#### 既定を `ocs` にする案を撤回した（2026-09-23）

`opencode` をシェル関数にして既定を `ocs` へ向けたが、**同日中に撤回した**。

**理由: sandbox が利便性を含めて完成していない。** 撤回時点で次が未対処。

- 起動ディレクトリの選び方で境界が消える（`cd ~ && ocs`）
- 外部レビューで裏が取れた欠陥 5 件
- 資格情報が境界内から読める
- セッション引き継ぎが未実装

**未完成のものを既定にすると、利用者は「境界がある」前提で振る舞う。**
実際には上の穴が開いているので、期待と実態が食い違う。既定は通常版に戻し、
`ocs` は明示的に選ぶものとして残す。

| | 撤回前 | 撤回後 |
| --- | --- | --- |
| `opencode` | シェル関数 → `ocs` | **素の OpenCode** |
| `ocs` | 直接呼んでもよい | **境界を使うときはこちら** |
| `--no-sandbox` | 素を呼ぶための明示フラグ | **廃止** |

完成の条件（＝再び既定にしてよい条件）は未定義。少なくとも上の 4 点が
片付いていることが前提になる。

### 実装で踏んだ癖（テストが見つけた）

| 症状 | 原因 |
| --- | --- |
| サブディレクトリで起動すると**親リポジトリ全体**を退避する | `git rev-parse --git-dir` は親を遡る。`--show-toplevel` を取り、パススペックで起動ディレクトリ以下に限る |
| tree から親のファイルが落ちる | plumbing を**サブディレクトリから**走らせると index に載るパスの基準がずれる。`toplevel` から走らせる |
| 退避した tar が**空になる** | `git archive` は **cwd をパスフィルタに使う**。絞り込み済みの tree をサブディレクトリから archive すると何も出ない。`toplevel` から走らせる |
| 起動ディレクトリ自体が `.gitignore` されていると `add` が失敗する | `check-ignore` で先に判定し、退避するものが無いとして扱う |
| 退避先が作れないとき Python の traceback が出る | `OSError` を `die()` で受け、対処（`--no-backup`）を示す |

### 境界の外から再開する手順（記録）

> **2026-09-24 に B4 で `ocs --handoff` を入れた。** 通常はそちらを使う
> （[引き継ぎの仕様](../spec/opencode-sandbox.md#セッションの引き継ぎ)）。
> 以下は移送せずに直接開く場合の手順で、下記の注意がそのまま残る。

隔離版のセッションは `OPENCODE_DB` で分かれているため、素の `opencode --continue`
では**候補に出ない**。境界なしで開くなら次。

```sh
OPENCODE_DB="$PWD/.opencode-sandbox/opencode.db" opencode --standalone --continue
```

> **`--standalone` が要る。** 付けないとバックグラウンドサービス（ホスト DB）へ
> 問い合わせるので、`OPENCODE_DB` が効かない。これを外して測り、
> 「ホスト DB の一覧」を「隔離版の一覧」と読み違えた。
>
> **再開先は通常版の設定になる。** `OPENCODE_CONFIG_DIR` も `XDG_DATA_HOME` も
> 差し替わらないので permission・policies・伏字化・境界の説明が全て効かない。
> 境界内で組み立てた文脈を境界なしのエージェントへ渡すことになる。

## 仕様への変更案

> **2026-09-23 に実態と突き合わせて更新した。** 案の段階で書いた表を段階 2〜6 の
> 実装中に更新しておらず、上の[実施計画](#実施計画)と矛盾していた（全件「未着手」の
> まま 12 件が完了していた）。

| 変更対象 | 変更前 → 変更後 | 理由 | 適用結果 |
| --- | --- | --- | --- |
| `README.md` | `snap install chezmoi` → 公式インストーラ | snap は bwrap 内で動かない | **完了**。`get.chezmoi.io` を使い、snap 版は「外す」案内に回した |
| **ランチャー** | （なし）→ `~/.local/bin/` に新設。`srt` で包んで `opencode --standalone` を起動 | **境界の本体** | **完了**。`~/.local/bin/ocs` |
| **境界の設定** | （なし）→ `~/.config/opencode/` の隣へ出力 | `denyRead` に `/mnt` `/tmp` を含む | **形を変えて完了**（下記） |
| **起動時チェック** | （なし）→ 挙動ベース・fail-closed | 値（`Seccomp:`）は証明にならない | **完了**。`~/.local/bin/ocs-boundary-check` |
| **`XDG_DATA_HOME`** | （なし）→ 永続領域へ向ける | **外すと安全網が消える** | **完了**。`.opencode-sandbox/data` |
| **隔離用 DB** | （なし）→ `OPENCODE_DB` で分ける | 他プロジェクトの会話を境界内へ入れない | **完了**。`.opencode-sandbox/opencode.db` |
| 隔離版の設定系統 | （なし）→ `OPENCODE_CONFIG_DIR` で分岐 | 緩和を隔離版に閉じ込める | **完了**。`~/.config/opencode-sandbox` |
| permission 規則 | 225 件 → 全件に処遇を割り当てて作り替え | ワークスペース相対の秘密 glob は**残す** | **完了**。通常版 225 件 / 隔離版 168 件 |
| `snapshots` | 既定のまま → 明示的に有効化 | ワークスペース破壊の復旧手段 | **完了** |
| `policies` | （なし）→ 運用上の禁止のみ | プロジェクト設定から解除させない | **完了**。4 件 |
| システムプロンプト | （なし）→ 境界の存在と範囲を伝える 7 項目 | 隠蔽と不在を区別できず誤診する | **完了**。`config_dir/AGENTS.md` へ出力 |
| `opencode.json` | `tools: {"grep": false, "glob": false}` | ファイル参照を shell へ寄せると、**ワークスペース内の秘密が素通りする**（段階 5 で撤回） | **不採用** |
| `guide-plugin` | `cat` → `read` の誘導 | **ワークスペース内の秘密に対して有効**。消すと `read` の deny が空振りする | **維持** |
| `guide-plugin` | 結果フィルタ 37 件 | `grep` / `glob` を無効化しないので引き続き要る | **維持** |
| `guide-plugin` | 伏字化のパス判定 34 件 | 境界内では不活性だが、通常版では現役。分岐を足す価値が無い | **維持** |
| sandbox の `denyWrite` | （なし）→ **次回の隔離起動を決める入力一式** | 書き換えて `chezmoi apply` されると保護が消える | **完了**。`protected` 6 件 |
| `[sandbox] shell_network_allow` | Claude 専用 → OpenCode にも適用。`opencode.ai` / `claude.ai` / `mise.jdx.dev` を追加 | 同じ許可リストを共有できる。失敗 53 件の解消 | **完了**。3 件とも反映（`claude.ai` は 2026-09-23 に追加） |
| GitHub の読み取り専用 PAT | （なし）→ **当面入れない** | 必要が生じるまで持ち込まない | **見送り** |
| サービスの起動 | 包む対象は**実行主体ごと**（`--standalone`） | 同一プロセスなので境界をまたがない | **完了** |
| `guide-plugin` が shell を包む | — | **不採用**（案 A / B とも破棄） | **不採用** |
| `bypass` エージェント | — | **不要**。復旧は境界の外の通常版 | **不採用** |

### 当初案と違う形になったもの

**境界の設定の出力先。** 「`~/.config/opencode/` の隣へ独立したファイル」を想定して
いたが、実際は `~/.config/opencode/guide-plugin/rules.json` の **`sandbox` キー**として
出ている。

結果として、**1 つの生成物を 2 人の客が共有する**構造になった。

| `rules.json` のキー | 読む人 |
| --- | --- |
| `guide` / `bypass_agents` / `read_deny` / `ask_description` / `redact` | `guide-plugin`（境界の**内側**） |
| **`sandbox`** | **`ocs`（境界の外側）** |

`ocs` は `sandbox` キーしか見ない。分離していないことの是非は未評価で、
**分けるなら `denyWrite` の保護対象も追随させる**必要がある
（現在は `guide-plugin` ディレクトリごと保護されている）。

## 終了結果

<!-- Done / Abandoned にするとき記入 -->
