#!/bin/zsh
# .zshenv>.zprofile>.zshrc>.zlogin

# Emacs mode
bindkey -e

if [[ -e "${HOME}/.local/bin/mise" ]]; then
    eval "$("${HOME}/.local/bin/mise" activate zsh)"
fi

#--------#
# common #
#--------#

SHELL_CONF=$HOME/.config/shell
if [ -e "$SHELL_CONF/shellrc.sh" ]; then
    # loads z.sh
    source "$SHELL_CONF/shellrc.sh"
fi

#-------------#
# Activations #
#-------------#

# activate all initially to prevent unexpected uninitialized caes

if command -v "fzf" >/dev/null 2>&1; then
    eval "$(fzf --zsh)"
    # common setting
    COMMON_FZF="${HOME}/.config/shell/fzf.sh"
    [ -e $COMMON_FZF ] && source $COMMON_FZF
fi

# # for Cline
# # see https://github.com/cline/cline/wiki/Troubleshooting-%E2%80%90-Shell-Integration-Unavailable#still-having-trouble
# [[ "$TERM_PROGRAM" == "vscode" ]] && . "$(code --locate-shell-integration-path zsh)"

#----------------#
# Plugin manager #
#----------------#

# Added by Zinit's installer
if [[ ! -f $HOME/.zinit/bin/zinit.zsh ]]; then
    print -P "%F{33}▓▒░ %F{220}Installing DHARMA Initiative Plugin Manager (zdharma/zinit)…%f"
    command mkdir -p "$HOME/.zinit" && command chmod g-rwX "$HOME/.zinit"
    command git clone https://github.com/zdharma-continuum/zinit "$HOME/.zinit/bin" && \
        print -P "%F{33}▓▒░ %F{34}Installation successful.%f%b" || \
        print -P "%F{160}▓▒░ The clone has failed.%f%b"
fi

# shellcheck source=/dev/null
source "$HOME/.zinit/bin/zinit.zsh"
autoload -Uz _zinit
(( ${+_comps} )) && _comps[zinit]=_zinit
# End of Zinit's installer chunk

#------------#
# completion #
#------------#

# based on https://zenn.dev/i9wa4/articles/2026-01-01-zsh-startup-optimization-zinit
# 依存順の調整: mise, zeno はプロンプトや fzf の表示崩れを防ぐため同期ロードに戻す
# compinit などの重い処理のみ遅延ロードする

# zeno.zsh (eager) - upstream デフォルトの zeno-plugin.zsh で同期初期化
# 起動高速化は ZENO_DISABLE_EXECUTE_CACHE_COMMAND=1 + mise run dotfiles-update に集約する
zinit ice lucid depth"1" blockf
zinit light yuki-yano/zeno.zsh
source "$HOME/.config/shell/zeno.zsh"

# (0a) 補完定義 - compinit の前に fpath へ追加
zinit ice wait'0a' lucid
zinit light zsh-users/zsh-completions

# (0a) mise 補完 - fpath へ追加 (compinit より前に必要)
if [[ -f ~/.local/share/zsh/site-functions/_mise ]]; then
    zinit ice wait'0a' lucid as"completion"
    zinit snippet ~/.local/share/zsh/site-functions/_mise
fi

# (0b) compinit - 補完の基盤 (650msかかるため遅延)
zinit ice wait'0b' lucid \
    atload'zicompinit; zicdreplay'
zinit light zdharma-continuum/null

# (0c) fzf-tab (必ず autosuggestions / fast-syntax-highlighting の前に読み込む)
zinit ice wait'0c' lucid
zinit light Aloxaf/fzf-tab

# (0d) ハイライト系
# zsh-autosuggestions は widget ラップ系プラグインの後にロードする必要があるため、
# fast-syntax-highlighting → zsh-autosuggestions の順に並べる (公式 README 準拠)。
# 起動初回プロンプトから suggestion を有効化するため両者とも eager で読み込む
# (turbo にすると plugin ロード後の precmd #2 で初めて widget がラップされるため、
#  起動直後 1 プロンプト分は suggestion が出ない)。
zinit ice lucid
zinit light zdharma-continuum/fast-syntax-highlighting
# zsh-autosuggestions の widget 配列 (ZSH_AUTOSUGGEST_*_WIDGETS) は
# upstream の config.zsh が `(( ! ${+VAR} )) &&` ガードでデフォルトを設定する。
# プラグインより先に typeset すると defaults が消えるため、
# 必ず source 完了後 (= zinit atload) に += する。
zinit ice lucid atload'
  ZSH_AUTOSUGGEST_CLEAR_WIDGETS+=(zeno-auto-snippet zeno-auto-snippet-and-accept-line)
'
zinit light zsh-users/zsh-autosuggestions

#---------------#
# other plugins #
#---------------#

# git
# ohmyzsh は lib/git.zsh のみ利用する。
# nocompletions を付けないと zinit が ohmyzsh 配下の _* を全て
# ~/.zinit/completions/ へリンクしてしまい、未ロードのプラグイン用補完が
# fpath に混ざる (例: zsh-z 用の _z が `_z:39: command not found: zshz` を出す)。
zinit ice wait'0e' lucid blockf nocompletions pick"lib/git.zsh"
zinit light ohmyzsh/ohmyzsh

#------#
# pure #
#------#

zinit ice compile'(pure|async).zsh' pick'async.zsh' src'pure.zsh'
zinit light sindresorhus/pure

#--------#
# docker #
#--------#

if command -v docker >/dev/null 2>&1; then
    zinit ice wait'0e' lucid
    zinit light felixr/docker-zsh-completion

    zinit ice wait'0e' lucid pick"docker-fzf.zsh"
    zinit load mnowotnik/docker-fzf-completion

    zinit ice wait'0e' lucid pick'docker-fzf'
    zinit load MartinRamm/fzf-docker

    zinit ice wait'0e' lucid as"program" pick"dfimage.bash"
    zinit load RyodoTanaka/.bash_extend
fi

#-----------------#
# OS dependencies #
#-----------------#

{{- if eq .chezmoi.os "darwin" }}
zle -N switch-arch
{{- end }}

#--------------------#
# ~/.zshrc.d loader  #
#--------------------#

if [[ -d ${HOME}/.zshrc.d ]]; then
    for f in ${HOME}/.zshrc.d/*.zsh(N); do
        source "$f"
    done
    unset f
fi
