# chezmoi apply 時に削除するターゲット
# https://www.chezmoi.io/reference/special-files/chezmoiremove/
#
# ここに書いたパスは、ソースから消えただけでは残ってしまう配備済みファイルを
# 明示的に片付けるためのもの。移行が全マシンに行き渡ったら削除してよい。
#
# 消したいものがディレクトリなら、配下のファイルを並べずにディレクトリを書く。
# ファイルだけ並べると中身が消えた後に空のディレクトリが残る (実測)。
# source に同名を復活させる場合は、先にここから消す。残すと apply 全体が
# inconsistent state で失敗する (実測)。

# 2026-08: critical_deny.py -> command_policy.py に改名 (中身は汎用の
# normalize + policy matcher で、critical_deny という名前が実態と合わなくなった)
.config/agents/critical_deny.py

# commit skill の補助スクリプト廃止。読み取り専用 git コマンドを直接実行する。
# scripts/ には他に何も無いのでディレクトリごと消す。
.claude/skills/commit/scripts

# 2026-09: ターン終了時に ADR 更新を促す Stop hook を廃止。
.claude/hooks/update-adr-on-stop.py

# 2026-09: adr skill を廃止し、ADR の作成・更新を checkpoint skill へ統合した。
# テンプレートは checkpoint skill の references へ移設済み。
.claude/skills/adr

# NOTE: __pycache__ は .chezmoiignore で管理対象外にしているため、ここに書いても
# 削除されない。古い .pyc が残って困る場合は手で削除する
# (`rm -rf ~/.config/agents/__pycache__`)。

# 2026-09: opencode-sandboxed -> ocs に改名 (毎回打つコマンドなので短くした)。
# 対になる境界チェックも ocs-boundary-check へ揃えた。
.local/bin/opencode-sandboxed
.local/bin/opencode-boundary-check
